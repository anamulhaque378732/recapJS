// math expression bye template literals

let price = 500;
let quantity = 30;

// console.log(
//   `total Price: ${price * quantity} tk \n pay the price to get it as your own`,
// );

function getDiscount(price) {
  return price * 0.1;
}
// console.log(
//   `You saved : ${getDiscount(price * quantity)} tk \n your total price: ${price * quantity - getDiscount(price)}`,
// );

let stock = 4;
// console.log(`status:${stock > 0 ? "in stock" + " " + stock : "Stock out"}`);
